// import { createContext, useContext } from "react";

// const FecthContext = createContext();
// function FetchProvider({ children }) {
//   return <FecthContext.Provider value={{}}>{children}</FecthContext.Provider>;
// }

// function useFetch() {
//   const context = useContext(FecthContext);
//   if (context === undefined)
//     throw new Error("FecthContext is used outside FetchProvider");
//   return context;
// }

// export { FetchProvider, useFetch };
